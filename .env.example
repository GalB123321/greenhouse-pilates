# =====================================================
# Sanity CMS Configuration - Example File
# =====================================================
# 
# IMPORTANT: Copy this file to .env.local and fill in your actual values
# cp .env.example .env.local
#
# How to get these values:
# 1. Create a Sanity account at https://www.sanity.io
# 2. Create a new project or use existing one
# 3. Go to https://www.sanity.io/manage
# 4. Select your project
# 5. Navigate to Settings > API
# 6. Copy your Project ID
# 7. Dataset is typically "production" for live content
#
# =====================================================

# Required: Your Sanity Project ID
NEXT_PUBLIC_SANITY_PROJECT_ID=replace_with_your_project_id

# Required: Your Sanity Dataset (usually "production")
NEXT_PUBLIC_SANITY_DATASET=production

# =====================================================
# Optional Configuration
# =====================================================

# Optional: For authenticated requests (server-side only)
# Create token at: Project > API > Tokens > Add API token
# Choose appropriate permissions (usually "Viewer" for read-only)
# SANITY_API_TOKEN=replace_with_your_token

# Optional: For Sanity Studio preview functionality
# Generate a random secret string for preview mode
# SANITY_STUDIO_PREVIEW_SECRET=replace_with_random_secret

# =====================================================
# Notes:
# - NEXT_PUBLIC_ prefix makes variables available in browser
# - Variables without prefix are server-side only
# - Never commit .env.local to version control
# - This example file is safe to commit
# =====================================================